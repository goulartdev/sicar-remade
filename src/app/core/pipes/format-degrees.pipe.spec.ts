import { FormatDegreesPipe } from './format-degrees.pipe';

describe('FormatDegreesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDegreesPipe();
    expect(pipe).toBeTruthy();
  });
});
